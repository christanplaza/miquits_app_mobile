<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView android:text="Therapist Name"
        android:id="@+id/rate_therapist_name"
        android:textSize="24dp"
        android:textColor="#0F0F0F"
        android:layout_marginTop="10dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"/>

    <TextView android:text="Massage Name"
        android:id="@+id/rate_massage_title"
        android:textSize="24dp"
        android:textColor="#0F0F0F"
        android:layout_marginTop="10dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"/>

    <RatingBar
        android:layout_marginTop="10dp"
        android:layout_marginBottom="10dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/dialog_ratingbar"
        android:layout_gravity="center"
        android:numStars="5"
        android:stepSize="1.0"
        android:isIndicator="false"/>

    <Button android:id="@+id/rank_dialog_button"
        android:layout_height="wrap_content"
        android:text="Submit"
        android:focusable="false"
        android:layout_margin="5dip"
        android:textColor="#FFF8C6"
        android:textSize="20dip"
        android:layout_width="fill_parent" />

</LinearLayout>